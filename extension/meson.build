fs = import('fs')

if fs.is_dir('/usr/lib/mozilla/native-messaging-hosts')
    install_data(
        'com.github.essmehdi.atay.json',
        install_dir: '/usr/lib/mozilla/native-messaging-hosts'
    )
endif