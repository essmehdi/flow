moduledir = join_paths(pkgdatadir, 'flow/preferences')

sources = [
    '__init__.py',
    'window.py',
    'category_row.py',
    'category_editor.py'
]

install_data(sources, install_dir: moduledir)