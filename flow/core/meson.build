moduledir = pkgdatadir / 'flow' / 'core'

sources = [
    '__init__.py',
    'controller.py',
    'download.py',
    'settings.py',
    'status_manager.py'
]

install_data(sources, install_dir: moduledir)