moduledir = join_paths(pkgdatadir, 'atay/components/preferences')

sources = [
    '__init__.py',
    'window.py',
    'category_row.py',
    'category_editor.py'
]

install_data(sources, install_dir: moduledir)